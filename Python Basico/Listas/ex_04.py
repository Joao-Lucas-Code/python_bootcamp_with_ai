# Listas
  # Estruturas de dados que armazenam coleções de elementos
  # Manter a sequencia dos dados em uma variável

numeros = [1, 2, 3, 4, 5]
letras = ['a', 'b', 'c', 'd', 'e']

numeros.extend(letras)  # Adiciona os elementos de uma lista a outra

print(numeros)  